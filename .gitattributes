# Auto-detect text files and handle line endings automatically
* text=auto

# Enforce Unix/Linux (LF) line endings for shell scripts and text files
# This is critical for wsl_ubuntu.sh to run correctly
*.sh      text eol=lf
*.gitignore text eol=lf
LICENSE   text eol=lf
README.md text eol=lf

# Enforce Windows (CRLF) line endings for PowerShell scripts
*.ps1     text eol=crlf
